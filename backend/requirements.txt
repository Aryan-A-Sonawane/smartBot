# ---- Web framework ----
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
pydantic==2.10.4
pydantic-settings==2.7.1

# ---- LLM ----
google-generativeai==0.8.3

# ---- RAG (retrieval) ----
# In-memory dense-vector index (cosine top-k) over Gemini embeddings. numpy keeps
# retrieval exact and low-latency for these corpus sizes without a heavy vector DB.
# Lower-bounded so it resolves with whatever ctranslate2/faster-whisper need.
numpy>=1.26

# ---- Extraction: PDF / image OCR / audio STT ----
pymupdf==1.25.1
pillow==11.1.0
pytesseract==0.3.13
faster-whisper==1.1.1

# ---- Web / cross-input ----
httpx==0.28.1
beautifulsoup4==4.12.3
lxml==5.3.0
youtube-transcript-api==0.6.3
# Lower-bounded, not pinned: YouTube breaks older releases, so build with the
# newest available. Used only by the YouTube audio fallback.
yt-dlp>=2024.11.04

# ---- Tooling / tests ----
pytest==8.3.4
pytest-asyncio==0.25.2
ruff==0.8.6
